# SOC Metrics Pipeline

A local proof of concept for an SOC metrics pipeline. A single Docker Compose stack
ingests deterministic mocked DFIR-IRIS-like, SIEM-like, and Shuffle-like source data into
`warehouse.raw`, rebuilds dashboard-ready tables in `warehouse.analytics` with plain SQL,
and serves them to Metabase.

The stack runs one PostgreSQL instance hosting three logical databases — `prefect`,
`metabase`, and `warehouse` — a Prefect server, a Prefect process worker that runs the
pipeline, and Metabase as the BI layer. A single parent flow, `soc_metrics_pipeline`,
orchestrates ingestion and analytics in order.

## Prerequisites

- Python 3.12
- [`uv`](https://docs.astral.sh/uv/)
- A container runtime that provides `compose` (Podman or Docker). The docs use
  `podman compose`; substitute `docker compose` if you use Docker.

## Environment configuration

Copy the example environment file before starting anything:

```bash
cp .env.example .env
```

The checked-in defaults are local-only placeholders that work as-is — no edits are
required to run the POC. Compose auto-loads `.env` and interpolates it into every service.
Secrets live in `.env` (gitignored); `.env.example` documents every required value, so the
stack needs no undocumented secrets.

## Quickstart

Host-side checks (optional, no containers needed):

```bash
uv sync
uv run ruff check .
uv run python -m compileall flows src
```

Bring up the stack:

```bash
cp .env.example .env
podman compose up -d postgres prefect-server metabase
```

This is only the first step. For the full operator workflow — registering the deployment,
starting the worker, running the parent flow, verifying warehouse data with `psql`, and
bootstrapping Metabase — follow [docs/local-development.md](docs/local-development.md).

## Repository layout

The repository uses `src/` as a Python source root. Runtime imports stay `db` and
`mock_sources`; there is no `src.*` package namespace.

## Documentation

- [docs/local-development.md](docs/local-development.md) — full local operator workflow and
  `psql` verification.
- [docs/metabase-setup.md](docs/metabase-setup.md) — Metabase admin bootstrap, warehouse
  connection, and dashboard path.

## Bootstrap Caveat

The Postgres bootstrap scripts under `docker/postgres/init/` run only when the
`postgres-data` volume is initialized for the first time. If you change logical
database roles, role passwords, or connection wiring, existing local volumes do
not automatically pick that up.

For changes in that category, either ship an explicit migration path for an
already-initialized cluster or reinitialize the local database with:

```bash
podman compose down -v
podman compose up -d
```

## Known limitations and out of scope

This is a deliberately minimal POC. The following are intentionally not included:

- **Mock data only** — no real DFIR-IRIS, SIEM, or Shuffle integrations. Source data is
  deterministic and generated by `src/mock_sources/`.
- **Single deployment, manual runs** — only `soc_metrics_pipeline` is deployed, with a
  manual run policy (no schedule). `ingest_raw` and `build_analytics` are code boundaries,
  not separate deployments.
- **No Redis, dbt, staging, or metadata schemas** — `warehouse` holds only the `raw` and
  `analytics` schemas for pipeline data; analytics is rebuilt with plain SQL.
- **Manual Metabase dashboards** — the bootstrap registers the read-only warehouse
  connection; dashboards and saved questions are created by hand.
- **First-run-only DB init** — see the Bootstrap Caveat above.
